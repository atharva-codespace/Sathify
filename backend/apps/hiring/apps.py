from django.apps import AppConfig


class HiringConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.hiring'
    verbose_name = 'Module 4 - Discovery & Hiring (recurring engagements)'
